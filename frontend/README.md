# ModelMaestro frontend

The frontend is intentionally left uninitialized until the UI stack is selected.

It will visualize orchestration runs, task dependencies, model decisions, cost, and live execution events.

